<x-student-layout title="{{ $studyGroup->name }}" page-title="{{ $studyGroup->name }}" breadcrumb="{{ $studyGroup->course?->title }}" :back-url="route('student.study-groups.index')">
    <div class="grid gap-5 lg:grid-cols-[minmax(0,1fr)_18rem]">
        <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="text-lg font-extrabold">Giới thiệu nhóm</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $studyGroup->description ?: 'Nhóm chưa có mô tả.' }}</p><a href="{{ route('study-groups.show', $studyGroup) }}" class="mt-5 inline-flex min-h-11 items-center justify-center rounded-xl bg-[#0056D2] px-5 text-sm font-bold text-white shadow-sm transition-all duration-200 hover:-translate-y-0.5 hover:bg-[#0046B8] hover:shadow-md active:translate-y-0 active:scale-95">Mở phòng thảo luận</a></section>
        <aside class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-extrabold">Thành viên ({{ $studyGroup->members_count }})</h2><ul class="mt-4 space-y-3">@foreach($studyGroup->members->take(8) as $member)<li class="flex items-center gap-3"><img src="{{ $member->avatarUrl() }}" alt="" class="h-9 w-9 rounded-lg object-cover"><span class="min-w-0 truncate text-sm font-semibold">{{ $member->name }}</span></li>@endforeach</ul></aside>
    </div>
</x-student-layout>
